---
title: sin
js: 
  - main.js
---
<div id="graph1"></div>

<pre><code>class Graph1 extends Somali.Scene 
  {
    get option() {
      return {id:"graph1", update: false}
    }
  
    constructor() {
      super();
    }
  
    createNodes(shapes, groups) {
      return {
        grid: groups.grid().labelX("θ").labelY("sin"),
        sin : groups.sin(),
      }
    }
  }
  
  new Graph1().build();</code></pre>